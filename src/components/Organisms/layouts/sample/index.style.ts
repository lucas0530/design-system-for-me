import styled from '@emotion/styled';

import * as Layout from '@/components/Atoms/Layout';

export const SampleHeader = styled(Layout.Header)``;
export const SampleBody = styled(Layout.Body)``;
export const SampleFooter = styled(Layout.Footer)``;
